import { Question } from '../types';

/**
 * Default questions database.
 * Sample questions have been completely removed as requested.
 * Teachers can import questions from Google Spreadsheet via Teacher Mode.
 */
export const DEFAULT_QUESTIONS: Question[] = [];
